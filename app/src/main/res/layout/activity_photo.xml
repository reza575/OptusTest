<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="photoshow"
            type="com.moeiny.reza.optustest.repository.model.PhotoShow"/>
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/rel_photoFragment_parent"
        android:background="@color/white"
        android:focusableInTouchMode="true"
        android:layout_height="match_parent">

        <RelativeLayout
            android:id="@+id/rl_fragmentphoto_albumid"
            android:layout_margin="@dimen/view_margin"
            android:layout_centerHorizontal="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <TextView
                android:textColor="@color/black"
                android:textSize="20sp"
                android:id="@+id/txt_fragmentphoto_albumid"
                android:text="Album ID : "
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

            <TextView
                android:id="@+id/txt_fragmentphoto_albumidvalue"
                android:textColor="@color/black"
                android:textSize="20sp"
                android:layout_toRightOf="@id/txt_fragmentphoto_albumid"
                android:text="@{photoshow.albumId}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

        </RelativeLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_fragmentphoto_photo"
            android:layout_below="@id/rl_fragmentphoto_albumid"
            android:layout_width="match_parent"
            android:paddingBottom="@dimen/txtbutton_Top_margin"
            android:layout_height="wrap_content">

        </androidx.recyclerview.widget.RecyclerView>

    </RelativeLayout>
</layout>
